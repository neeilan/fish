<%- include ./header -%>

<form id="form-fish" action="/fish/<%= fish.id %>" method="post" class="form-horizontal">
    <div class = "row">
    <div class="form-group">
        <label class="col-sm-2 control-label">Order</label>
        <div class="col-sm-2">
            <input name="order" value="<%= fish.order %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Family</label>
        <div class="col-sm-2">
            <input name="family" value="<%= fish.family %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Genus</label>
        <div class="col-sm-2">
            <input name="genus" value="<%= fish.genus %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Species</label>
        <div class="col-sm-2">
            <input name="species" value="<%= fish.species %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Subspecies</label>
        <div class="col-sm-2">
            <input name="subspecies" value="<%= fish.subspecies %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Initial Tube Label</label>
        <div class="col-sm-2">
            <input name="initial_tube_label" value="<%= fish.initial_tube_label %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Individual</label>
        <div class="col-sm-2">
            <input name="individual" value="<%= fish.individual %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">DNA Prep #</label>
        <div class="col-sm-2">
            <input name="dna_prep_number" value="<%= fish.dna_prep_number %>"  class="form-control">
        </div>
    </div>
    
        <div class="form-group">
        <label class="col-sm-2 control-label">Museum Voucher CAT #</label>
        <div class="col-sm-2">
            <input name="museum_voucher_cat_number" value="<%= fish.museum_voucher_cat_number %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Crampton EOD #</label>
        <div class="col-sm-2">
            <input name="crampton_eod_number" value="<%= fish.crampton_eod_number %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Country</label>
        <div class="col-sm-2">
            <input name="country" value="<%= fish.country %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Locality</label>
        <div class="col-sm-2">
            <input name="locality" value="<%= fish.locality %>"  class="form-control">
        </div>
    </div>
    
        <div class="form-group">
        <label class="col-sm-2 control-label">Latitude</label>
        <div class="col-sm-2">
            <input name="latitude" value="<%= fish.latitude %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Longitude</label>
        <div class="col-sm-2">
            <input name="longitude" value="<%= fish.longitude %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Date collected</label>
        <div class="col-sm-2">
            <input name="date_collected" value="<%= fish.date_collected %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Notes</label>
        <div class="col-sm-2">
            <input name="notes" value="<%= fish.notes %>"  class="form-control">
        </div>
    </div>
    
    <div class="form-group">
        <label class="col-sm-2 control-label">Added by</label>
        <div class="col-sm-2">
            <input name="added_by" value="<%= fish.added_by %>"  class="form-control">
        </div>
    </div>
</div>
<button type="submit" class="btn btn-primary">Save</button>
<button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteFishModal">Delete</button>
</form>
</br><br/>

<!-- Modal -->
<div id="deleteFishModal" class="modal fade" role="dialog">
  <div class="modal-dialog">

    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Add fish</h4>
      </div>
      <div class="modal-body">
        <form id="form-delete-fish" action="/deletefish/<%= fish.id %>" method="post" class="form-horizontal">
            This action cannot be undone! Are you sure you want to delete this fish?
            <button type="submit" class="btn btn-danger">Yes, delete this fish</button>
            <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>

        </form>
      </div>
    </div>

  </div>
</div>
<!-- End Modal -->


<div class="row">
      <div class="col-xs-12">
          <form action="/fileupload" method="POST">
            <div class="panel panel-default">
              <div class="panel-body">
                <span class="glyphicon glyphicon-cloud-upload"></span>
                <h4>Files</h4>
                <div class="progress">
                  <div class="progress-bar" role="progressbar"></div>
                </div>
                <input id="upload-input" type="file" name="uploads[]" multiple="multiple" style="display:none"></br>
                <button class="btn btn-lg upload-btn" type="button">Upload File</button>
              </div>
            </div>
        </form>
      </div>
</div>


<div class = "row">
    <% if (groups && groups.length > 0) { %>

    Add this fish to a group:
    <div class="form-group">
        <form id="form-fishgroup" action="/addfish/<%= fish.id %>" method="post" class="form-horizontal">

          <select name="group" class="form-control" id="sel1">
                  <% groups.forEach(function (group) { %>
                    <option value=<%= group.id %>><%= group.name %></option>
                <% }) %>
          </select>
          
          <button class="btn btn-primary" type="submit">Add</button>
        <% } else { %>
        
            <tbody>
                <tr><td colspan="3">You do not currently have any groups to add fish. You can create groups <a href = "/groupslist">here</a></td></tr>
            </tbody>
            
        <% } %>
        </form>
    </div>

    
    
</div>


<br/><br/>

<%- include ./footer -%>

<script>

$('.upload-btn').on('click', function (){
    $('#upload-input').click();
    $('.progress-bar').text('0%');
    $('.progress-bar').width('0%');
});


$('#upload-input').on('change', function(e){

  var files = $(this).get(0).files;

  if (files.length > 0){
    // create a FormData object which will be sent as the data payload in the
    // AJAX request
    var formData = new FormData();

    // loop through all the selected files and add them to the formData object
    for (var i = 0; i < files.length; i++) {
      var file = files[i];

      // add the files to formData object for the data payload
      formData.append('uploads[]', file, file.name);
    }

    $.ajax({
      url: window.location.href + '/fileupload',
      type: 'POST',
      data: formData,
      processData: false,
      contentType: false,
      success: function(data){
          alert('upload successful!\n' + data);
      },
      xhr: function() {
        // create an XMLHttpRequest
        var xhr = new XMLHttpRequest();

        // listen to the 'progress' event
        xhr.upload.addEventListener('progress', function(evt) {

          if (evt.lengthComputable) {
            // calculate the percentage of upload completed
            var percentComplete = evt.loaded / evt.total;
            percentComplete = parseInt(percentComplete * 100);

            // update the Bootstrap progress bar with the new percentage
            $('.progress-bar').text(percentComplete + '%');
            $('.progress-bar').width(percentComplete + '%');

            // once the upload reaches 100%, set the progress bar text to done
            if (percentComplete === 100) {
              $('.progress-bar').html('Done');
            }
          }
        }, false);
        return xhr;
      }
    });

  }
});
</script>
